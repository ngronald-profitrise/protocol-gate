\* TLC model-checker configuration for ProtocolGate.tla
\* Run with: tlc -config ProtocolGate.cfg ProtocolGate.tla
\* Function-valued constants are bound via `<-` substitution to definitions in
\* the module, because TLC .cfg files cannot express function literals directly.

SPECIFICATION Spec

CONSTANT Actors <- ActorSet
CONSTANT Capabilities <- CapFn

INVARIANT Safety

CHECK_DEADLOCK FALSE
